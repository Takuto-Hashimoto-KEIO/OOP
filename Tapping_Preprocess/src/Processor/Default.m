classdef TapDataEditor
    % このクラスの概要をここに記述

    properties
    end

    methods(Access=public)
        function obj = TapDataLoader()
        end
    end

    methods
        % このクラスのすべての処理を一貫して実行
        function [cfg, result] = _data(obj, cfg, result)
            % 出力を整理
        end
    end

    % % を処理
    % function obj = edit_(obj)
    % % 出力を整理
    % end
end

